# Load environment variables from .env
dotenv .env

# Activate Python virtual environment
source .venv/bin/activate

# Set Claude Code to use this project's venv
export CLAUDE_PYTHON_PATH="$(pwd)/.venv/bin/python"