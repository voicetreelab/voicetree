{
  "name": "vt-fake-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "test": "node ../../scripts/run-remote.mjs pnpm --filter vt-fake-agent run test:local",
    "test:local": "vitest run"
  },
  "dependencies": {
    "@vt/vt-rpc": "*"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
