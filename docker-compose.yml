services:
  voicetree:
    image: ghcr.io/voicetreelab/voicetree:latest
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: voicetree
    ports:
      - "6080:6080"
    volumes:
      - project:/home/vt/project
      - claude-config:/home/vt/.config/claude
    env_file:
      - .env
    shm_size: "1gb"
    restart: unless-stopped

volumes:
  project:
  claude-config:
