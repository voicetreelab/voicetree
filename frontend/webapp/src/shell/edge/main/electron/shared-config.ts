/**
 * Shared configuration constants for VoiceTree
 * Used by both Electron main process and renderer process
 */

export const BACKEND_PORT: 8001 = 8001;
export const BACKEND_BASE_URL: "http://localhost:8001" = `http://localhost:${BACKEND_PORT}`;
