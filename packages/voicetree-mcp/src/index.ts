// Public API surface for @vt/voicetree-mcp.
// Hosts the MCP server (HTTP) and tool implementations. Both Electron and
// vt-mcpd consume this via the entrypoint or the stable internal subpaths
// declared in package.json#exports.
//
// Phase C1 scaffold: this barrel becomes populated in C2 when the MCP server
// runtime is moved out of webapp/src/shell/edge/main/mcp-server/.
export {}
