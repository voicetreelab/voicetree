{
  "max": 125,
  "note": "2026-06-06: 123 -> 125 (+2). graph-model is the top fan-in package; the live-gardening prevention layer crosses two new distinct symbols into it: getSubFolderPaths (the create_graph show-folders response lists a destination's existing sub-folders) and DEFAULT_SUBGRAPH_ERROR_THRESHOLD (vt graph garden sources its block-threshold warning from the canonical settings default instead of a drifting literal). The folder_child_count_limit rule's getFolderChildNodeIds + getFolderIdentityNoteId were already imported across this boundary, so add nothing. 2026-06-06: 122 -> 123 (+1). The PR merge against current dev now includes app-config settings sanitization using graph-model's reserved prompt env key helper, bringing the CI-tested graph-model runtime fan-in to 123. The hierarchical-agent daemon edge is still kept at 28 by deriving its default leaf from the already-imported flattenAgentTree helper instead of importing resolveDefaultAgent as another value symbol. 2026-06-06: 121 -> 122 (+1). Hierarchical agent commands also need the shared mapAgentTreeByCommand command rewriter in the webapp settings/menu paths so command edits update the defining tree node at any depth. Kept the daemon-side edge at 28 by deriving its default leaf from the already-imported flattenAgentTree helper instead of importing resolveDefaultAgent as another value symbol. 2026-06-05: 120 -> 121 (+1 net). Hierarchical agent commands: graph-model now exposes the shared agent-tree resolver (flattenAgentTree, resolveDefaultAgent, mapAgentTreeByCommand) imported by both the webapp and the daemon, minus the removed flat getDefaultAgent. Surface was kept minimal — the path label lives on ResolvedAgent, the category check + resolvable-command set are inlined at call sites, and the UI-only tree-edit ops live in the webapp — so only +1 distinct symbol crosses the boundary. 2026-06-03: 117 -> 120 (+3). graph-model is the top fan-in package; the create_graph child_count_limit + graph_complexity_limit gates added 3 tunable settings defaults (DEFAULT_MAX_CHILDREN_PER_NODE, DEFAULT_COMPLEXITY_WARN_SCORE, DEFAULT_COMPLEXITY_BLOCK_SCORE) imported by createGraphTool."
}
