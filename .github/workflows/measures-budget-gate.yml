# AUTO-GENERATED by packages/measures/scripts/gen-workflows.ts
# from tier_N/_workflow.ts. Do not edit by hand.
# Run `npm run gen:workflows` to regenerate.
# Required GitHub contexts are derived from tier WorkflowSpec.protection.
name: "Measures (generated)"

on:
  workflow_dispatch: {}
  pull_request:

jobs:
  tier4-precheck:
    name: tier4-precheck
    if: "github.base_ref == 'main'"
    runs-on: ubuntu-latest
    outputs:
      reason: "${{ steps.decide.outputs.reason }}"
      should_run: "${{ steps.decide.outputs.should_run }}"
    steps:
      - uses: actions/checkout@v4
      - name: decide
        env:
          GH_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          LARGE_CEILING: 10000
          LARGE_FLOOR: 1000
          PR_NUMBER: "${{ github.event.pull_request.number }}"
          REPO: "${{ github.repository }}"
          STALE_DAYS: 7
        run: |
          set -euo pipefail
          ADDED=$(gh api "repos/$REPO/pulls/$PR_NUMBER" --jq '.additions // 0')
          ADDED=${ADDED:-0}
          echo "PR added $ADDED lines"

          if [ "$ADDED" -lt "$LARGE_FLOOR" ]; then
            echo "should_run=false" >> "$GITHUB_OUTPUT"
            echo "reason=PR is small ($ADDED < $LARGE_FLOOR lines)" >> "$GITHUB_OUTPUT"
            echo "::notice title=Tier 4 skipped::PR adds $ADDED lines (< $LARGE_FLOOR floor)"
            exit 0
          fi

          if [ "$ADDED" -ge "$LARGE_CEILING" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "reason=huge PR ($ADDED >= $LARGE_CEILING lines)" >> "$GITHUB_OUTPUT"
            echo "::notice title=Tier 4 running::huge PR ($ADDED lines)"
            exit 0
          fi

          LAST=""
          for sha in $(gh api "repos/$REPO/commits?sha=main&per_page=100" --jq '.[].sha'); do
            LAST=$(gh api "repos/$REPO/commits/$sha/check-runs" \
                   --jq '.check_runs[] | select(.name=="Main CI / Full" and .conclusion=="success") | .completed_at' \
                   | head -n1)
            [ -n "$LAST" ] && break
          done

          if [ -z "$LAST" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "reason=no successful Main CI / Full found on main in last 100 commits" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          AGE_DAYS=$(( ( $(date +%s) - $(date -d "$LAST" +%s) ) / 86400 ))
          echo "last successful Main CI / Full on main: $LAST (${AGE_DAYS}d ago)"
          if [ "$AGE_DAYS" -gt "$STALE_DAYS" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "reason=stale (${AGE_DAYS}d > ${STALE_DAYS}d) and PR is $ADDED lines" >> "$GITHUB_OUTPUT"
            echo "::notice title=Tier 4 running::stale (${AGE_DAYS}d > ${STALE_DAYS}d)"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
            echo "reason=Tier 4 fresh (${AGE_DAYS}d <= ${STALE_DAYS}d) and PR under huge threshold" >> "$GITHUB_OUTPUT"
            echo "::notice title=Tier 4 skipped::fresh (${AGE_DAYS}d old)"
          fi

  tier-0-pre-commit-lint:
    name: tier-0-pre-commit-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-0-pre-commit-lint checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=0 \
            --only=blackbox-tests-lint,root-lint
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-0-pre-commit-lint
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-0-pre-commit-policy:
    name: tier-0-pre-commit-policy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-0-pre-commit-policy checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=0 \
            --only=baseline-commit-isolation
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-0-pre-commit-policy
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-0-pre-commit-static:
    name: tier-0-pre-commit-static
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-0-pre-commit-static checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=0 \
            --only=agent-instructions-sync,e2e-taxonomy
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-0-pre-commit-static
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-1-coverage:
    name: tier-1-coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-1-coverage checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=1 \
            --only=ci-coverage
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-1-coverage
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-1-e2e:
    name: tier-1-e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: Install Playwright chromium
        working-directory: webapp
        run: npx playwright install --with-deps chromium
      - name: "Run tier-1-e2e checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=1 \
            --only=e2e-browser-smoke,e2e-tier1
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-1-e2e
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-1-health:
    name: tier-1-health
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-1-health checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=1 \
            --only=systems-health
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-1-health
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-1-structure:
    name: tier-1-structure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-1-structure checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=1 \
            --only=orange-gate,relative-import-depth,relative-path-depth
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-1-structure
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-1-typecheck:
    name: tier-1-typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-1-typecheck checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=1 \
            --only=webapp-check
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-1-typecheck
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-2-contract:
    name: tier-2-contract
    needs: [tier-0-pre-commit-lint, tier-0-pre-commit-policy, tier-0-pre-commit-static, tier-1-coverage, tier-1-e2e, tier-1-health, tier-1-structure, tier-1-typecheck]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-2-contract checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=2 \
            --sequential \
            --only=graph-model-public-api-contract,graph-state-public-api-contract
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-2-contract
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-2-e2e:
    name: tier-2-e2e
    needs: [tier-0-pre-commit-lint, tier-0-pre-commit-policy, tier-0-pre-commit-static, tier-1-coverage, tier-1-e2e, tier-1-health, tier-1-structure, tier-1-typecheck]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: Install Playwright chromium
        working-directory: webapp
        run: npx playwright install --with-deps chromium
      - name: "Run tier-2-e2e checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=2 \
            --sequential \
            --only=e2e-tier2-browser,e2e-tier2-electron-critical,graph-db-client-e2e-system,graph-db-server-e2e-system,graph-tools-e2e-system
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-2-e2e
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-2-fuzz:
    name: "tier-2-fuzz (${{ matrix.check_id }})"
    needs: [tier-0-pre-commit-lint, tier-0-pre-commit-policy, tier-0-pre-commit-static, tier-1-coverage, tier-1-e2e, tier-1-health, tier-1-structure, tier-1-typecheck]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        check_id:
          - fuzz-editor-sync
          - fuzz-graph-delta
          - fuzz-graph-state-invariants
          - fuzz-session-state
          - fuzz-system-lifecycle
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-2-fuzz check"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=2 \
            --sequential \
            --only=${{ matrix.check_id }}
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-2-fuzz-${{ matrix.check_id }}
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-2-lint:
    name: tier-2-lint
    needs: [tier-0-pre-commit-lint, tier-0-pre-commit-policy, tier-0-pre-commit-static, tier-1-coverage, tier-1-e2e, tier-1-health, tier-1-structure, tier-1-typecheck]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-2-lint checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=2 \
            --only=verify-cytoscape-rules,webapp-lint
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-2-lint
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-2-unit:
    name: tier-2-unit
    needs: [tier-0-pre-commit-lint, tier-0-pre-commit-policy, tier-0-pre-commit-static, tier-1-coverage, tier-1-e2e, tier-1-health, tier-1-structure, tier-1-typecheck]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-2-unit checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=2 \
            --only=agent-runtime-unit,graph-db-server-unit,graph-model-unit,graph-state-unit,graph-tools-unit,voicetree-mcp-unit,vt-fake-agent-unit,webapp-unit
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-2-unit
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-3-e2e:
    name: tier-3-e2e
    needs: [tier-2-contract, tier-2-e2e, tier-2-fuzz, tier-2-lint, tier-2-unit]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: Install Playwright chromium
        working-directory: webapp
        run: npx playwright install --with-deps chromium
      - name: "Run tier-3-e2e checks"
        run: |
          xvfb-run -a -s "-screen 0 1280x1024x24" \
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=3 \
            --sequential \
            --only=e2e-tier2-electron
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-3-e2e
          path: health-dashboard/reports/checks/
          retention-days: 1

  tier-4-analyzers:
    name: tier-4-analyzers
    needs: [tier-3-e2e, tier4-precheck]
    if: "github.base_ref == 'main' && needs.tier4-precheck.outputs.should_run == 'true'"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: "Run tier-4-analyzers checks"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/capture-ci-checks.ts \
            --tier-max=4 \
            --only=dead-code,duplication,mutation-graph-model,mutation-graph-state,tier-time-budget-gate
      - name: Upload check reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-tier-4-analyzers
          path: health-dashboard/reports/checks/
          retention-days: 1

  budget-gate:
    name: budget-gate
    needs: [tier-0-pre-commit-lint, tier-0-pre-commit-policy, tier-0-pre-commit-static, tier-1-coverage, tier-1-e2e, tier-1-health, tier-1-structure, tier-1-typecheck, tier-2-contract, tier-2-e2e, tier-2-fuzz, tier-2-lint, tier-2-unit, tier-3-e2e, tier-4-analyzers, tier4-precheck]
    if: "always()"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm ci
      - name: Download all check reports
        uses: actions/download-artifact@v4
        with:
          pattern: reports-*
          path: health-dashboard/reports/checks/
          merge-multiple: true
      - name: "Run tier budget gate (max tier 4)"
        env:
          GITHUB_BASE_REF: "${{ github.base_ref }}"
          MEASURES_CONDITIONAL_JOBS_BY_BASE_REF: "{\"main\":[\"tier-4-analyzers\"]}"
          MEASURES_CONDITIONAL_PRECHECK_BY_JOB_ID: "{\"tier-4-analyzers\":\"tier4-precheck\"}"
          MEASURES_REQUIRED_JOBS_BY_BASE_REF: "{\"dev\":[\"budget-gate\",\"tier-0-pre-commit-lint\",\"tier-0-pre-commit-policy\",\"tier-0-pre-commit-static\",\"tier-1-coverage\",\"tier-1-e2e\",\"tier-1-health\",\"tier-1-structure\",\"tier-1-typecheck\",\"tier-2-contract\",\"tier-2-e2e\",\"tier-2-fuzz\",\"tier-2-lint\",\"tier-2-unit\"],\"main\":[\"budget-gate\",\"tier-0-pre-commit-lint\",\"tier-0-pre-commit-policy\",\"tier-0-pre-commit-static\",\"tier-1-coverage\",\"tier-1-e2e\",\"tier-1-health\",\"tier-1-structure\",\"tier-1-typecheck\",\"tier-2-contract\",\"tier-2-e2e\",\"tier-2-fuzz\",\"tier-2-lint\",\"tier-2-unit\",\"tier-3-e2e\"]}"
          MEASURES_WORKFLOW_NEEDS_JSON: "${{ toJson(needs) }}"
        run: |
          node --no-warnings=ExperimentalWarning --experimental-strip-types \
            packages/measures/src/_runners/check-tier-budgets.ts
