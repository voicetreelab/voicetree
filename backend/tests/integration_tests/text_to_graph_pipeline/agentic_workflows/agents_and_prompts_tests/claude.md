Tests in this folder should NOT use mocks.

We want to test the actual LLM behaviour.

We want generally behavioural tests, or input -> output tests. Testing at the highest level of abstraction that is relevant
i.e. either a method, prompt, agent, or module. 